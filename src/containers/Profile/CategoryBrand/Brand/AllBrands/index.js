import {View, Text} from 'react-native';
import React from 'react';

const AllBrandsScreen = props => {
  return (
    <View
      style={{
        flex: 1,
        flexDirection: 'row',
        justifyContent: 'center',
        alignItems: 'center',
      }}>
      <Text style={{color: '#000'}}>All Brands Screen</Text>
    </View>
  );
};

export default AllBrandsScreen;
